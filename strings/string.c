#include <stdio.h>
#include <string.h>
int main()
{
    // initialize a string.
    //  char arr[]="Subhojit Nath is a good boy\0";
    //  int i=0;
    //  while(arr[i]!='\0'){
    //      printf("%c",arr[i]);
    //      i++;
    //  }

    // char str[] = "physicswallah";
    // int i=0;
    // str[1]=97;
    // str[0]='M';
    // while(str[i]!='\0'){
    //     printf("%c",str[i]);
    //     i++;
    // }

   // printf("%s", str);
   char str[40];
 //scanf("%s",str); //only the first word will be considered
   //gets(str); //entire sentence can be taken as input
   printf("%s",str);

    return 0;
}