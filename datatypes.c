#include <stdio.h>
int main()
{
    int Age = 22;
    float pi = 3.14;
    char hastag = '#';

    // printf("age is %d \n",age);
    // printf("value of pi is %f \n",pi);
    // printf("hastage %c \n",hastag);
    // printf() is used to give output in c

    // scanf() is used to take input in c
    int age;
    printf("age is");
    scanf("%d", &age);
    printf("age is : %d \n", age);

    return 0;
}